# Copia este fichero a .env (sin el .example) y pon tu clave real.
# Jev / TypeSafe: la clave se saca en https://console.typesafe.ai/keys
TYPESAFE_API_KEY=
# Opcionales
TYPESAFE_DEFAULT_MODEL=jev-latest
# TYPESAFE_BASE_URL=https://api.typesafe.ai
# TYPESAFE_TIMEOUT=90
